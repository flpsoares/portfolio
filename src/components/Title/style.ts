import styled from 'styled-components'

export const Container = styled.div`
  font-size: 32px;
  font-weight: 600;
  font-style: italic;
  margin-bottom: 60px;

  span {
    color: var(--tertiary);
  }
`
